name: publish regul patch
on:
    schedule:
        - cron: '*/25 * * * *'
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Use Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '20.x'
            - run: npm ci
            - run: npm build
            - run: npm version patch
            - nun: git push --follow-tags

